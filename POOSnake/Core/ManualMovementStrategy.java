package Core;

public class ManualMovementStrategy implements MovementStrategy {
    // private int direction;

    public ManualMovementStrategy(int direction) {
        // this.direction = direction;
    }

    @Override
    public void move(Snake snake) {
        // Implementação do movimento conforme a direção fornecida
        // snake.setDirection(this.direction);
        // snake.move(this.direction);
    }
}